<Window x:Class="LEditor.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"            
        xmlns:common="clr-namespace:LEditor.Common"
        xmlns:utils="clr-namespace:LEditor.Utils"
        xmlns:local="clr-namespace:LEditor.Views"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="800" Width="1200"
        prism:ViewModelLocator.AutoWireViewModel="True">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Column="0" prism:RegionManager.RegionName="{x:Static common:RegionNames.TeamSettingRegion}"/>
        <ContentControl Grid.Column="1" prism:RegionManager.RegionName="{x:Static common:RegionNames.SelectedPlayerRegion}"/>
        <ContentControl Grid.Column="2" prism:RegionManager.RegionName="{x:Static common:RegionNames.WaitPlayerRegion}"/>
    </Grid>
</Window>
